---
logging: 
  log_dir: dataset_log/
  log_filename: datasets.log
  log_name: LARGE_SENTS_CHAR
data:
  source_type: csv
  raw_data: stories.csv
  dataset_name: large-sents-c.pkl
  vocab_size: 8000
  num_rows: 7400 # Keeps dataset < 100MB so it can be hosted on GitHub
  num_examples: 
  mode: sentences
  token_level: characters